name: mpflutter_template
description: A new MPFlutter project.
publish_to: "none"
version: 1.0.0+1
playbox:
  name: 模板工程
  description: 
  icon: school
  background: blue
environment:
  sdk: ">=2.18.0 <3.0.0"
dependencies:
  flutter: "0.20.0"
  flutter_web_plugins: "0.20.0"
  mpcore: "0.20.0"
  mp_build_tools: "0.20.0"

  dio: ^4.0.0
  flutter_hooks: ^0.18.0
  # flutter_svg: ^1.1.4
  # freezed_annotation: ^2.1.0
  hooks_riverpod: ^2.1.1
  html: ^0.15.0
  # json_annotation: ^4.6.0
  # pubspec_parse: ^1.2.1
  # riverpod_annotation:
  shimmer: ^2.0.0
dev_dependencies:
  build_runner: ^2.0.0
  build_verify: ^3.0.0
  # flutter_test:
  freezed: ^2.1.0
  # json_serializable: ^6.3.0
  mockito: ^5.0.0
  riverpod_generator:
dependency_overrides:
  # meta: ^1.7.0 # 必须添加这个依赖
  flutter: "0.20.0"
  mpcore: "0.20.0"
  flutter_web_plugins: "0.20.0"
  vector_math: ^2.1.2
  collection: ^1.17.0